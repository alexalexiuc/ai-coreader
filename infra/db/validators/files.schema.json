{
  "title": "FilesDoc",
  "description": "Represents a stored file and its processing status",
  "additionalProperties": false,
  "required": [
    "createdAt",
    "updatedAt",
    "originalName",
    "mimeType",
    "size",
    "storagePath",
    "storageName",
    "status"
  ],
  "properties": {
    "_id": {
      "description": "File unique identifier",
      "bsonType": "objectId"
    },
    "createdAt": {
      "description": "Creation timestamp",
      "bsonType": "date"
    },
    "updatedAt": {
      "description": "Last update timestamp",
      "bsonType": "date"
    },
    "originalName": {
      "description": "Original file name as uploaded by the user",
      "bsonType": "string"
    },
    "mimeType": {
      "description": "MIME type of the file",
      "bsonType": "string"
    },
    "size": {
      "minimum": 0,
      "description": "File size in bytes",
      "bsonType": "int"
    },
    "storagePath": {
      "description": "Storage path of the file",
      "bsonType": "string"
    },
    "storageName": {
      "description": "Internal storage file name",
      "bsonType": "string"
    },
    "percentage": {
      "minimum": 0,
      "maximum": 100,
      "description": "Processing completion percentage (0-100)",
      "bsonType": [
        "double",
        "int"
      ]
    },
    "status": {
      "description": "Processing status of the file",
      "enum": [
        "pending",
        "processing",
        "processed",
        "failed"
      ],
      "bsonType": "string"
    }
  },
  "bsonType": "object"
}