# Hooks

This folder contains React hooks used across the app.

- `useLibrary` (in `useLibrary.ts`) encapsulates the library page state (search, filter, sort, view) and helper functions.

Add future hooks here and export them from `index.ts` for easier imports (e.g. `import { useFoo } from '@/hooks'`).
